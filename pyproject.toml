[tool.poetry]
name = "algo-harjoitustyo"
version = "0.1.0"
description = ""
authors = ["Mullil <eetu.taatila@gmail.com>"]
readme = "README.md"
packages = [{ include = "src" }]

[tool.poetry.dependencies]
python = ">=3.12"
numpy = ">=2.4.1,<3.0.0"
python-mnist = ">=0.7,<0.8"
pylint = ">=4.0.4,<5.0.0"
autopep8 = ">=2.3.2,<3.0.0"
coverage = ">=7.13.2,<8.0.0"

[tool.poetry.group.dev.dependencies]
pytest = ">=9.0.2,<10.0.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
