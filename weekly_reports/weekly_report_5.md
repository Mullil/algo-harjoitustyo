# Weekly report 5

This week I implemented the testing feature for the model, and now the model accuracy is always evaluated after
training using the test set. The algorithm seems to work correctly, as the testing accuracy is almost 96% after just one epoch with 2 layers that have a hidden dimension of 128.

 The models can also be saved into directories with their parameters and metadata after training, and the data in these directories can then be loaded to use an already trained model.

 This week I learned to save and load the trained neural network, although this was quite trivial so there was not a lot of learning involved.

 This week there was nothing unclear or difficult. Next I will start extending the tests and perhaps add some optimizations to training the network. However, I think tests take priority. At the time of writing this I still have not gotten the peer review, so new tasks might arise after getting the peer review.

This week I used around 4 hours on the project.